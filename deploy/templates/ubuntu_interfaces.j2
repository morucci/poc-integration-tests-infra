auto lo
    iface lo inet loopback
auto eth0
    iface eth0 inet static
    address {{ item.value.ip }}
    netmask 255.255.255.0
    dns-nameservers {{ network.nameserver }}
    gateway {{ network.gateway }}
